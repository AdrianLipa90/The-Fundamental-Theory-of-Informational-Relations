# TIR × IDT — Information-Area Curvature Typing

Status: `EXACT_CP1_AREA_BERRY_TYPING / PHYSICAL_LENGTH_SCALE_BINDING_OPEN / RFC_COUPLING_DOWNSTREAM`

Source pin:

- IDT `main` commit `647f1652edde59d9bfd7e075fb6ed5bf02aab2fc`
- IDT gate `formalism/01K_temporal_information_curvature_interface.md`

## 1. Dimensional correction

TIR keeps Shannon entropy and KL relative information as dimensionless information scalars once the logarithm base is fixed. The inverse-square type is generated by relational areal normalization.

For the Bloch/CP1 reference chart,

\[
ds^2_{FS}
=\frac14\left(d\theta^2+\sin^2\theta\,d\varphi^2\right),
\]

so the dimensionless Fubini--Study area two-form is

\[
\boxed{
d a_{FS}
=\frac14\sin\theta\,d\theta\wedge d\varphi.
}
\]

For the standard spin-1/2 Berry convention,

\[
\boxed{
\mathcal F_B
=\pm\frac12\sin\theta\,d\theta\wedge d\varphi
=\pm2\,d a_{FS}.
}
\]

The sign tracks orientation/gauge convention. Therefore the full Bloch sphere carries

\[
\boxed{a_{FS}(S^2)=\pi},
\qquad
\boxed{\int_{S^2}\mathcal F_B=\pm2\pi}.
\]

This matches the reviewed TIR Berry normalization in which a full-sphere solid angle `4pi` maps to Berry phase magnitude `2pi`.

## 2. Physical relational area calibration

The CP1/Fubini--Study area above is dimensionless. Introduce one admitted length calibration `ell_R > 0` and define

\[
\boxed{
\mathcal A_{\rm rel}
:=\ell_R^2\,a_{FS}.
}
\]

Then

\[
\boxed{[\mathcal A_{\rm rel}]=L^2}.
\]

Under `ell_R -> s ell_R`,

\[
\mathcal A_{\rm rel}\to s^2\mathcal A_{\rm rel}.
\]

This is the TIR area-calibration interface required by IDT 01K.

## 3. Information-area scalar

Let the IDT/TIR relative-information scalar in bits be

\[
\mathcal I_\pi
=D_{\rm KL}^{(2)}(p\|\pi).
\]

In nats,

\[
\mathcal J_\pi=(\ln2)\mathcal I_\pi.
\]

The relational information-area scalar is therefore

\[
\boxed{
\Xi_I
=\frac{\mathcal J_\pi}{\mathcal A_{\rm rel}}
=\frac{\ln2}{\ell_R^2 a_{FS}}\,\mathcal I_\pi.
}
\]

Hence

\[
\boxed{[\Xi_I]=L^{-2}}.
\]

Using the canonical TIR normalization

\[
\kappa=\frac{\ln2}{24\pi},
\]

this becomes the exact identity

\[
\boxed{
\Xi_I
=\frac{24\pi\kappa}{\ell_R^2 a_{FS}}\,\mathcal I_\pi.
}
\]

The substitution uses the independently fixed TIR `kappa`; it introduces no new normalization parameter beyond the physical length scale `ell_R`.

## 4. Polyhedral/refinement form

For a polyhedral CP1 cell decomposition `P` with oriented faces `f`, define

\[
a_{FS}^{(P)}
=\sum_f a_{FS}(f),
\]

with face areas determined from the FS metric or equivalently from Berry flux through

\[
\boxed{
\int_f\mathcal F_B=\pm2a_{FS}(f).
}
\]

The physical relational area is

\[
\boxed{
\mathcal A_{\rm rel}^{(P)}
=\ell_R^2 a_{FS}^{(P)}.
}
\]

A refinement sequence is admissible for RFC when the relevant integrated area/flux invariants converge and the rank-three spatial metric gate is preserved.

## 5. Export contract to IDT and RFC

TIR exports:

```text
cp1_dimensionless_area_form = da_FS
berry_curvature              = F_B = ±2 da_FS
length_calibration            = ell_R > 0
physical_relational_area      = A_rel = ell_R^2 a_FS
inverse_area_information      = Xi_I = ln(2) I_pi / A_rel
kappa_form                    = Xi_I = 24 pi kappa I_pi / A_rel
```

IDT supplies the temporal evolution `dXi_I/dtau_int`. RFC owns the field-level coupling of `Xi_I` into the dynamic `Lambda0` scalar.

## 6. Claim typing

Exact within the declared CP1 convention:

- FS area form;
- Berry/FS factor `F_B = ±2 da_FS`;
- full-sphere values `a_FS = pi` and Berry flux magnitude `2pi`;
- `A_rel = ell_R^2 a_FS` dimensional typing;
- inverse-square scaling of `Xi_I`;
- `ln2 = 24 pi kappa` substitution.

Open physical inputs:

- empirical or derived value of `ell_R`;
- selection/refinement rule for the physical polyhedral area carrier;
- RFC `Xi_I -> Lambda0` coupling and action-level field equations.
