# TIR CKM Half-Kernel Product-Flow Uniqueness Audit v0.7

Status: `EXACT_OPERATOR_NONUNIQUENESS_FROM_TRACE / EXACT_MONOIDAL_GENERATOR_THEOREM / PHYSICAL_INDEPENDENCE_OPEN_POSTDICTIVE`

Date: 2026-09-14

## Scope

v0.6 established the exact scalar readout

\[
\tau(\mathcal O_{12})=b+a\kappa,
\qquad b=\frac29,\quad a=\frac27,
\]

with the half-boundary selecting the seven-dimensional packet.  This audit asks a narrower question: does that scalar result uniquely determine the operator, or is an additional dynamical principle required?

The answer is exact:

- the scalar trace does **not** identify a unique operator;
- the Kronecker sum is the unique infinitesimal generator **if** the two sectors evolve as an independent tensor-product flow;
- TIR has not yet derived that physical independence theorem for CKM, so the physical selector remains open.

No new CKM observable is consumed.

## 1. Product projector algebra

Let

\[
P=P_F^{(9)},\qquad Q=P_F^{(7)},
\]

with normalized product trace

\[
\tau(P)=b=\frac29,
\qquad
\tau(Q)=a=\frac27,
\qquad
\tau(PQ)=ab.
\]

The commuting algebra generated by the two nontrivial projectors has basis

\[
\{I,P,Q,PQ\}.
\]

These four elements are linearly independent: evaluation on the four joint eigensectors `(p,q) in {0,1}^2` gives a 4x4 matrix of determinant one.

## 2. Exact zero-trace correlated direction

Define the centered interaction

\[
\boxed{C=(P-bI)(Q-aI).}
\]

Expanding,

\[
C=PQ-aP-bQ+abI.
\]

Because the trace state factorizes,

\[
\boxed{\tau(C)=ab-a\,b-b\,a+ab=0.}
\]

Since `C` is nonzero, the one-parameter family

\[
\boxed{\mathcal O_{12}^{(\delta)}=\mathcal O_{12}+\delta C}
\]

has exactly the same normalized trace as v0.6 for every real `delta`:

\[
\tau(\mathcal O_{12}^{(\delta)})=b+a\kappa.
\]

Therefore the exact CKM scalar readout alone cannot identify the underlying operator.  This is an operator-identifiability no-go, not a failure of the v0.6 arithmetic.

## 3. Conditional monoidal generator theorem

Assume the bulk and half-boundary sectors are dynamically independent at the infinitesimal level, so that their one-parameter flow is

\[
U_{63}(t)=U_9(t)\otimes U_7(t),
\]

with

\[
U_9(t)=e^{tP},
\qquad
U_7(t)=e^{t\kappa Q}.
\]

Then

\[
\begin{aligned}
U_{63}(t)
&=(I+tP+O(t^2))\otimes(I+t\kappa Q+O(t^2))\\
&=I+t(P\otimes I+\kappa I\otimes Q)
+t^2\kappa P\otimes Q+O(t^2_{\rm local},t^3).
\end{aligned}
\]

Hence the derivative at the identity is uniquely

\[
\boxed{
\left.\frac{dU_{63}}{dt}\right|_{t=0}
=P\otimes I+\kappa I\otimes Q.
}
\]

That is precisely the v0.6 Kronecker-sum operator.

The bilinear product `P tensor Q` appears canonically only at second order in the expansion of the independent product flow.  A first-order correlated term requires an additional interaction generator, such as `delta C`, and therefore violates the assumed independent monoidal flow.

## 4. What is and is not unique

Exact theorem:

\[
\boxed{
\text{independent tensor-product flow}
\Longrightarrow
\text{unique infinitesimal generator }P\otimes I+\kappa I\otimes Q.
}
\]

Exact no-go:

\[
\boxed{
\text{normalized trace }b+a\kappa
\not\Longrightarrow
\text{unique operator}.
}
\]

Still open is the physical step

\[
\boxed{
\text{TIR flavour/connection dynamics}
\Longrightarrow
\text{independent monoidal product flow for the 12 channel}.
}
\]

The existing A2 and incidence layers establish the carrier and projector data, but they do not yet prove this independence principle.

## 5. Epistemic ledger

`EXACT`:

- linear independence of `I,P,Q,PQ`;
- existence of the nonzero zero-trace correlated direction `C`;
- nonuniqueness of the operator under scalar trace data alone;
- first-order expansion of the tensor-product flow;
- uniqueness of the Kronecker-sum generator under the monoidal-independence assumption;
- the fact that the canonical product cross term enters at second order.

`OPEN / CONDITIONAL`:

- deriving monoidal independence from the TIR connection, holonomy, or information-flow law;
- excluding a genuine first-order correlated flavour interaction physically;
- prospective validation of the refined CKM formula.

The v0.6 scalar mechanism remains valid.  v0.7 narrows its remaining selector debt instead of promoting it prematurely.

Validator:

`TIR/validation/tir_ckm_half_kernel_product_flow_uniqueness_audit_v0_7.py`
