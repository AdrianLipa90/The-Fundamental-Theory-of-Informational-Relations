# TIR × Secret-of-a-Half / PhaseNav / NOEMA Integration v0.7.0

Status: `SOFTWARE_DEPENDENCY_CLOSURE_PASS_CANDIDATE`.

This snapshot advances the v0.5 integration without changing any open or failed
physical claim.

## Canonical executable parents

- PhaseNav hard canon `1.2.0`, dependency layer `0.7.0`:
  `54f65f2ca7d35cdd98f0ab8984cc1a8d74444a96`
- NOEMA dependency/provenance contract:
  `42a0a8916e81ca27f2213bf0f28538f046c2e89a`

The PhaseNav parent contains the executable T^36 dependency firewall. NOEMA
stores the commit/status/provenance projection and has no authority to promote a
scientific claim.

## What v0.7 closes

The previous statement that all ten role/orientation assignments were simply
open is now too coarse. The dependency chain is split explicitly:

1. `(h,a,b,c)` slot-role routing — `STRUCTURAL_ROLE_ROUTING_PASS`;
2. directed orbital sign convention — `IMPLEMENTED_PROJECT_ORBITAL_RULE`;
3. relational-gradient operator —
   `IMPLEMENTED_SOURCE_OPERATOR_CANDIDATE_TIR_BINDING`;
4. prospective binding of those operators to the TIR coefficient slots — `OPEN`.

Thus the **software/structural operator vocabulary is implemented and typed**,
while the physical assignment theorem is not.

## Dimensions

The intrinsic coefficient state is exactly four-dimensional:

`(h,a,b,c)`.

The canonical PhaseNav transport envelope is 36D. These spaces are not
identified and a 36D route cannot be used as a coefficient parent.

## Scientific debt retained

Unchanged:

- gauge-boson relations remain `OPEN TENSION`;
- Higgs formula remains `RETROSPECTIVE REVISION`;
- neutron EDM remains `PHYSICAL FAIL`;
- Collatz quarter-power mass bridge remains `OPEN`;
- common up-sector baseline remains `RESTRICTED NO-GO`;
- frozen separable family remains `PROSPECTIVE`;
- common likelihood/covariance remains open;
- RG-consistent electroweak comparison remains open;
- retrospective Collatz stopping-orientation pattern is not canon and requires a
  prospective test;
- Secret-of-a-Half `SOH-C004` and `SOH-C005` remain open.

## Proof/data firewall

Measured masses and Yukawa couplings are scoring-only and cannot be parents of
the role/orientation derivation. Vectorization or NOEMA memory state cannot
promote an open, retrospective, or failed claim.

## Executable bridge

`dependency_bridge.py` emits a deterministic integration receipt and verifies the
status boundary using only the Python standard library.

This file supersedes the v0.5 snapshot as the current integration status, but the
v0.5 directory remains historical provenance and is not overwritten.
