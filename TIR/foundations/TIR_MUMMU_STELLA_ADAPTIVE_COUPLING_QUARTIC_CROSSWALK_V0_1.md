# TIR MUMMU Stella Adaptive-Coupling Quartic Crosswalk v0.1

Status: `EXACT_CONDITIONAL_STELLA_SHELL_CROSSWALK / EXACT_SHARED_STRUCTURE_FACTOR / EXACT_SHARED_QUARTIC_C4_COEFFICIENT / SOURCE_EDGE_BINDING_OPEN / PHYSICAL_BINDING_OPEN`

Date: 2026-09-23

## 1. Purpose

Two previously separate MUMMU surfaces contain the same cosine kernel:

1. the Stella structure factor,
2. the adaptive coupling variational law.

This theorem shows that, once a local coupling shell is explicitly bound to the
canonical Stella relation directions, the scalar Hebbian coupling force is
exactly the Stella structure factor.

Consequently the existing quartic anisotropy

[
-rac1{108}C_4
]

appears automatically in the adaptive-coupling lane.

## 2. Canonical local relation carrier

The TIR spatial branch supplies the intrinsic affine relation

[
mathcal E_{xy}
=
2(ho_y-ho_x)
in
operatorname{Herm}_0(2)
cong
mathbb R^3,
]

with invariant metric

[
langle A,Bangle
=
rac12operatorname{Tr}(AB).
]

No external spatial origin is required.

For the Stella shell use the eight normalized relation directions

[
oxed{
mathcal V
=
left{
rac1{sqrt3}(s_x,s_y,s_z):
s_iin{-1,+1}
ight}.
}
]

This is the same shell used by the existing MUMMU quartic theorem.

## 3. Admitted shell binding

This theorem is conditional on a local source binding that identifies eight
coupling channels with the eight Stella directions.

Let one normalized scalar shell-coupling coordinate be (ar g), distributed
equally over the shell:

[
g_{mathbf n}
=
rac{ar g}{8}.
]

Let the local relational phase increment be generated by a covector

[
qin V^*
]

through

[
oxed{
DeltaPhi_{mathbf n}
=
q(mathbf n)
=
mathbf qcdotmathbf n
}
]

in an orthonormal Pauli frame.

This is a local relation-space statement. It is not yet a production binding of
the PNCS 36-channel graph to physical spatial edges.

## 4. Hebbian shell potential

The adaptive coupling potential contains the directional term

[
mathcal F_H
=
-lambda_H
sum_{mathbf ninmathcal V}
g_{mathbf n}
cos(DeltaPhi_{mathbf n}).
]

Under the equal-shell coordinate,

[
mathcal F_H
=
-lambda_Har g
left[
rac18
sum_{mathbf ninmathcal V}
cos(mathbf qcdotmathbf n)
ight].
]

Define

[
S(mathbf q)
=
rac18
sum_{mathbf ninmathcal V}
e^{imathbf qcdotmathbf n}.
]

Antipodal symmetry makes (S) real, hence

[
oxed{
-rac{partialmathcal F_H}{partialar g}
=
lambda_H S(mathbf q).
}
]

Thus the normalized scalar coupling force is exactly the Stella structure
factor multiplied by the signed activity rate.

## 5. Exact factorization

Because the signs separate,

[
oxed{
S(mathbf q)
=
cosrac{q_x}{sqrt3}
cosrac{q_y}{sqrt3}
cosrac{q_z}{sqrt3}.
}
]

Therefore the adaptive-coupling shell force has the exact finite form

[
oxed{
-rac{partialmathcal F_H}{partialar g}
=
lambda_H
cosrac{q_x}{sqrt3}
cosrac{q_y}{sqrt3}
cosrac{q_z}{sqrt3}.
}
]

No new coefficient is introduced.

## 6. Shared quartic anisotropy

Define

[
r^2=q_x^2+q_y^2+q_z^2
]

and

[
C_4(mathbf q)
=
q_x^4+q_y^4+q_z^4
-rac35r^4.
]

The existing exact asymptotic is

[
S(mathbf q)
=
1
-rac{r^2}{6}
+rac{r^4}{120}
-rac1{108}C_4(mathbf q)
+O(r^6).
]

Therefore

[
oxed{
-rac{partialmathcal F_H}{partialar g}
=
lambda_H
left[
1
-rac{r^2}{6}
+rac{r^4}{120}
-rac1{108}C_4(mathbf q)
+O(r^6)
ight].
}
]

The first orientation-selecting scalar term in the adaptive shell force is

[
oxed{
-rac{lambda_H}{108}
C_4(mathbf q).
}
]

This is exactly the same Stella quartic harmonic and coefficient as the
geometric carrier term in the MUMMU quartic seam, multiplied only by the source
activity rate (lambda_H).

## 7. Structural meaning

The identity does not arise from fitting one model to another.

Both lanes reduce to the same finite relation-shell character:

[
oxed{
rac18
sum_{mathbf ninmathcal V}
cos(qcdot n).
}
]

Hence:

[
oxed{
	ext{Stella carrier anisotropy}
}
]

and

[
oxed{
	ext{Stella-bound adaptive coupling anisotropy}
}
]

are two readouts of one underlying finite cosine kernel.

This is an exact mathematical crosswalk conditional on the shell binding.

## 8. Activity bifurcation

Using

[
lambda_H
=
0.1(A-	frac12),
]

the adaptive quartic term is

[
-rac{0.1(A-rac12)}{108}C_4.
]

Therefore:

[
A=rac12
Longrightarrow
	ext{adaptive Stella quartic force}=0,
]

and crossing the activity hyperplane reverses its sign.

The geometric Stella structure factor itself does not disappear; only its
signed adaptive-coupling actuation coefficient does.

## 9. What remains open

The theorem does not establish:

- a native PNCS map from its 36 coupling coordinates to eight Stella relation
  directions;
- that one physical neighborhood has exactly this shell;
- that (q) is a physical momentum/wavevector;
- that the coupling scalar (ar g) is a physical metric component;
- that the adaptive flow is realized by neutrinos or gravity.

The required next source object is a typed local edge-shell binding

[
oxed{
mathcal B_{m shell}:
	ext{PNCS coupling channels}
	o
	ext{canonical TIR relation directions}.
}
]

Only after such a binding may this exact crosswalk be promoted from conditional
mathematics to source-bound MUMMU dynamics.

## 10. Claim ledger

| Statement | Status |
|---|---|
| canonical relation carrier is (operatorname{Herm}_0(2)congmathbb R^3) | `EXACT IMPORTED` |
| eight Stella directions give exact factorized structure factor | `EXACT IMPORTED` |
| equal-shell Hebbian force equals (lambda_H S(q)) | `EXACT CONDITIONAL` |
| first anisotropic adaptive shell term is (-lambda_H C_4/108) | `EXACT CONDITIONAL ASYMPTOTIC` |
| geometric and adaptive quartic terms use the same finite cosine kernel | `EXACT CONDITIONAL CROSSWALK` |
| native PNCS 36-channel → Stella-shell binding exists | `OPEN / NOT FOUND` |
| (q) is physical momentum | `OPEN / NOT CLAIMED` |
| adaptive coupling is physical spacetime metric | `OPEN / NOT CLAIMED` |

## 11. Validation

Deterministic validator:

`TIR/validation/tir_mummu_stella_adaptive_coupling_quartic_crosswalk_v0_1.py`

Static receipt:

`TIR/validation/TIR_MUMMU_STELLA_ADAPTIVE_COUPLING_QUARTIC_CROSSWALK_VALIDATION_V0_1.json`


## 12. Native harmonic-mask binding firewall

The tempting source multiplicity split

[
36=8	imes3+2	imes6
]

has been audited in

`TIR_MUMMU_PNCS_HARMONIC_MASK_STELLA_BINDING_NOGO_V0_1.md`.

Although eight harmonic-mask classes occur with multiplicity three, those eight
classes do not form a Hamming cube, no one-bit deletion gives eight unique
three-bit labels, and no rank-three linear quotient over (mathbb F_2) is
injective on them.

Therefore the current PNCS harmonic-mask table cannot serve as the missing
Stella-shell binding by cardinality or linear bit reduction alone.

The conditional quartic crosswalk remains valid, but its shell-binding premise
must be discharged by a different source invariant.
