# TIR MUMMU QHTRI Proper-Time Scale Calibration Closure v0.1

Status: `EXACT_CONDITIONAL_SCALE_RECOVERY / NATIVE_PNCS_TIME_RECEIPT_BRANCH_GREEN / MODEL_CALIBRATION_CLOSED / FUNDAMENTAL_ACTUATION_LAW_OPEN`

Date: 2026-09-23

## 1. Purpose

The QHTRI coefficient sector has been reduced to

[
H
=
sK_chi,
qquad
K_chi
=
coschi,widehat D
+
sinchi,widehat J.
]

The trajectory-shape gate can calibrate (chi) without knowing (s).

This theorem closes the remaining **model-level identifiability** of (s) once
the coordinate-time trajectory is bound to a typed proper-time receipt.

It does not derive why a physical system should choose the calibrated
((s,chi)).

## 2. Time-binding source

The TIR typed-time and trajectory-receipt surfaces are:

- `TIR_MUMMU_QHTRI_PNLF_TYPED_TIME_BRIDGE_NOGO_V0_1.md`;
- `TIR_MUMMU_QHTRI_PNLF_TRAJECTORY_RECEIPT_BINDING_V0_1.md`.

A native PNCS read-only emitter exists on:

`AdrianLipa90/PhaseNav-Natural-Coding-System`

branch

`feat/mummu-qhtri-pnlf-trajectory-binding-v01-20260923`

at source-pinned head

`7e1a2dab237ec741ec29d1da19007520c9755d8d`.

The emitter binds per step

[
dt_{m HTRI}=dt_{m coord}
]

and

[
oxed{
d	au
=
g,dt_{m coord},
qquad
g>0.
}
]

Promotion of that PNCS branch to `main` is not implied here.

## 3. Coordinate-time scaling law

Let (F_u(chi)) denote the local pair-imbalance derivative generated by the
normalized Hamiltonian (K_chi):

[
oxed{
F_u(chi)
:=
left.
rac{du}{dt}
ight|_{H=K_chi}.
}
]

For

[
H=sK_chi,
]

the Schrödinger equation gives the exact local scaling

[
oxed{
rac{du}{dt}
=
sF_u(chi).
}
]

This follows because the first state derivative is linear in (H).

## 4. Proper-time derivative

With the typed time bridge

[
d	au=g,dt,
]

one has

[
rac{du}{d	au}
=
rac1g
rac{du}{dt}.
]

Therefore

[
oxed{
rac{du}{d	au}
=
rac{s}{g}F_u(chi).
}
]

If

[
F_u(chi)
e0,
]

the Hamiltonian scale is uniquely recovered as

[
oxed{
s
=
g
rac{du/d	au}{F_u(chi)}.
}
]

For positive (s,g), the measured derivative and (F_u) must have the same
sign. Otherwise the receipt/model pair is inconsistent and must fail closed.

## 5. Two-stage identifiability closure

The model-level calibration can therefore be performed without joint fitting.

### Stage A — shape

Use scale-free trajectory invariants

[
R_1
=
rac{|Omega|}{|dot u|},
qquad
R_2
=
rac{mathcal W}{|Omega|^3}
]

to recover (chi) on an admitted invertible branch.

### Stage B — clock scale

Use the independent typed proper-time receipt (g) and one nonzero local
proper-time derivative to recover

[
s
=
g
rac{du/d	au}{F_u(chi)}.
]

Hence, conditionally on the admitted trajectory branch,

[
oxed{
(	ext{trajectory shape},	ext{proper-time receipt})
Longrightarrow
(chi,s).
}
]

## 6. Why this is stronger than raw QHTRI fitting

Without the proper-time binding,

[
(s,t)
mapsto
(lambda s,t/lambda)
]

leaves the same unitary path.

The time receipt breaks that degeneracy by fixing which coordinate interval
corresponds to which proper-time interval.

Thus:

[
oxed{
s	ext{ is gauge-like before clock binding, identifiable after clock binding.}
}
]

## 7. Reparameterization compatibility

This does not contradict geometric holonomy invariance.

The one-form obeys

[
Omega_	au,d	au
=
Omega_t,dt,
]

so the integrated projective holonomy of the same path is independent of the
parameterization.

Scale calibration instead uses the **rate** at which the receipt-bound path is
traversed.

Path geometry and clock calibration remain distinct typed observables.

## 8. Pinned deterministic witness

For the pinned PNCS deterministic branch, the normalized default angle gives

[
F_u(chi_{m default})
approx
-5.8759260167	imes10^{-3}.
]

Choose an independent positive proper-time factor (g) and a Hamiltonian scale
(s).  If the generated proper-time derivative is

[
rac{du}{d	au}
=
rac{s}{g}F_u,
]

the inverse formula reconstructs (s) to floating-point precision.

The validator checks several ((s,g)) pairs and rejects the singular
(F_u=0) endpoint.

## 9. Remaining fundamental gate

Model identifiability is now closed, but source authority is not.

There is still no source law of the form

[
oxed{
	ext{independent physical/source quantity}
longrightarrow
(s,chi)
}
]

that predicts the Hamiltonian parameters before observing/calibrating the
trajectory.

GREMLIN's actuation firewall therefore remains active.

The current status is:

[
oxed{
(s,chi)
=
	ext{identifiable/calibratable model parameters}
}
]

not

[
oxed{
(s,chi)
=
	ext{derived fundamental constants}.
}
]

## 10. Claim ledger

| Statement | Status |
|---|---|
| (du/dt=sF_u(chi)) | `EXACT` |
| (d	au=g,dt) under admitted typed-time receipt | `EXACT CONDITIONAL SOURCE BRIDGE` |
| (du/d	au=(s/g)F_u) | `EXACT` |
| (s=g(du/d	au)/F_u) for (F_u
e0) | `EXACT` |
| shape + typed clock identifies ((chi,s)) on admitted branch | `EXACT CONDITIONAL / MODEL LEVEL` |
| native PNCS receipt emitter exists on pinned feature branch | `IMPLEMENTED / HOSTED GREEN` |
| model calibration predicts a fundamental Hamiltonian | `REFUTED` |
| independent actuation law predicts ((s,chi)) | `OPEN` |

## 11. Validation

Deterministic validator:

`TIR/validation/tir_mummu_qhtri_proper_time_scale_calibration_v0_1.py`

Static receipt:

`TIR/validation/TIR_MUMMU_QHTRI_PROPER_TIME_SCALE_CALIBRATION_VALIDATION_V0_1.json`
