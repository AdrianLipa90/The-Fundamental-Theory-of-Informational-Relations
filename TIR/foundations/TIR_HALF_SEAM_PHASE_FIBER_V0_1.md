# TIR Half-Seam Phase Fiber v0.1

Status: `EXACT_GEOMETRIC_LIFT_CANDIDATE`

Scope: TIR-only lift of the binary half-seam from probability space into the pure two-state quantum carrier. This module identifies the information-theoretic point \(p=1/2\) with a one-dimensional relative-phase fiber and makes the Bloch-sphere geometry explicit.

## 1. Base space: binary probability coordinate

For the first distinction \(\{N,S\}\), write

\[
\mathbf p(u)=(1-u,u),
\qquad
0\le u\le1.
\]

Pole exchange is

\[
J(u)=1-u.
\]

Its unique fixed point is

\[
\boxed{u_\star=\frac12.}
\]

At this base point,

\[
p_N=p_S=\frac12,
\qquad
H_2=\ln2.
\]

This is the relational half-seam already established by the TIR first-distinction chain.

## 2. Quantum lift of the same probability point

A normalized pure state of the two-state carrier can be written, after removal of global phase, as

\[
|\psi(u,\varphi)\rangle
=\sqrt{1-u}\,|N\rangle
+e^{i\varphi}\sqrt{u}\,|S\rangle,
\qquad
\varphi\in\mathbb R/2\pi\mathbb Z.
\]

Projection to probabilities is

\[
\pi:\ |\psi(u,\varphi)\rangle
\longmapsto
(1-u,u).
\]

For \(0<u<1\), the probability coordinate forgets the relative phase. In particular, over the half-seam

\[
\pi^{-1}\!\left(\frac12,\frac12\right)
=
\left\{
\frac{|N\rangle+e^{i\varphi}|S\rangle}{\sqrt2}
:\varphi\in S^1
\right\}.
\]

Therefore

\[
\boxed{
\text{probability half-seam }\frac12
\quad\longrightarrow\quad
\text{quantum phase fiber }S^1.
}
\]

The scalar half is the base coordinate; the unresolved coherent information above it is a relative phase circle.

## 3. Bloch-sphere realization

For

\[
|\psi(u,\varphi)\rangle
=\sqrt{1-u}|N\rangle
+e^{i\varphi}\sqrt{u}|S\rangle,
\]

the Bloch vector is

\[
\mathbf r(u,\varphi)
=\left(
2\sqrt{u(1-u)}\cos\varphi,
2\sqrt{u(1-u)}\sin\varphi,
1-2u
\right).
\]

Its norm is exactly

\[
|\mathbf r|^2
=4u(1-u)+(1-2u)^2
=1.
\]

Thus pure states lie on

\[
\boxed{S^2.}
\]

At the half-seam \(u=1/2\),

\[
\boxed{
\mathbf r\left(\frac12,\varphi\right)
=(\cos\varphi,\sin\varphi,0),
}
\]

which is exactly the equator.

Hence the first distinction has the geometric structure

\[
\boxed{
N\;\text{pole}
\quad\longleftrightarrow\quad
S^1_{\rm half}\;\text{equator}
\quad\longleftrightarrow\quad
S\;\text{pole}.
}
\]

The information-theoretic midpoint is therefore an entire coherent equatorial fiber after the quantum phase coordinate is restored.

## 4. Pole exchange on the phase fiber

Let the pole-exchange operator satisfy

\[
X|N\rangle=|S\rangle,
\qquad
X|S\rangle=|N\rangle.
\]

Applied to the half-fiber state,

\[
X|\psi_{1/2}(\varphi)\rangle
=\frac{|S\rangle+e^{i\varphi}|N\rangle}{\sqrt2}.
\]

Removing the global factor \(e^{i\varphi}\), this is projectively equivalent to

\[
|\psi_{1/2}(-\varphi)\rangle.
\]

Thus pole exchange acts on the half-fiber as

\[
\boxed{\varphi\mapsto-\varphi\pmod{2\pi}.}
\]

Its projective fixed phases obey

\[
\varphi=-\varphi\pmod{2\pi},
\]

hence

\[
\boxed{\varphi\in\{0,\pi\}.}
\]

These are the two exchange eigen-directions on the equator.

## 5. Arithmetic-geometric closure

A5 measures the equatorial phase geometry by winding:

\[
\boxed{
n=\frac1{2\pi}\oint d\varphi\in\mathbb Z.
}
\]

A6 reads the same closure in complex form:

\[
z=e^{i\varphi},
\qquad
z^n=1.
\]

For a primitive \(n\)-fold phase closure,

\[
\boxed{
\varphi=\frac{2\pi k}{n}.
}
\]

Thus the same half-fiber simultaneously carries

\[
\boxed{
S^1
\longleftrightarrow
U(1)
\longleftrightarrow
\text{winding}
\longleftrightarrow
n\in\mathbb N.
}
\]

This is the direct local interface between A4/A5/A6 and the binary quantum distinction.

## 6. Entry to the Schrödinger branch

Introduce a continuous parameter \(\tau\) and a strongly continuous phase flow on the half fiber,

\[
\varphi=\varphi(\tau).
\]

For a constant phase rate,

\[
\varphi(\tau)=\varphi_0+\omega\tau,
\]

so

\[
|\psi(\tau)\rangle
=\frac{|N\rangle+e^{i(\varphi_0+\omega\tau)}|S\rangle}{\sqrt2}.
\]

This is a one-parameter unitary orbit. By the standard unitary-generator theorem, a strongly continuous unitary flow admits a self-adjoint generator. With physical-time identification and Hamiltonian generator, the usual Schrödinger equation follows.

The TIR dependency edge is therefore

\[
\boxed{
\frac12
\rightarrow
S^1_{\rm phase}
+\text{continuous unitary flow}
\rightarrow
\text{generator}
\rightarrow
\text{Schrödinger branch}.
}
\]

The half-seam determines the equal-modulus carrier; the dynamical phase law is an additional layer.

## 7. Entry to the Heisenberg branch

The `N/S` distinction chooses one Bloch axis. A second distinction axis is represented by another diameter of \(S^2\). Distinct quantum axes are generated by different Pauli operators.

For example,

\[
[\sigma_x,\sigma_z]=-2i\sigma_y\ne0.
\]

Therefore the same two-pole carrier supports incompatible distinction frames. With self-adjoint observables, the Robertson inequality yields the Heisenberg uncertainty structure.

The TIR edge is

\[
\boxed{
S^2
+\text{multiple distinction axes}
\rightarrow
\text{noncommutativity}
\rightarrow
\text{Heisenberg branch}.
}
\]

## 8. Information geometry of the lift

The binary probability coordinate sees only the vertical Bloch coordinate

\[
z=1-2u.
\]

At \(u=1/2\),

\[
z=0.
\]

All equatorial phases therefore project to the same probability point:

\[
\boxed{
\pi:S^1_{\rm equator}\to\left\{\frac12\right\}.
}
\]

This is the exact sense in which a single information-theoretic half-point becomes a fuzzy/coherent family after the quantum phase degree of freedom is restored.

## 9. Crosslink packet

TIR exports the enlarged half-seam packet

```text
probability_base      = 1/2
binary_entropy        = ln2
quantum_fiber         = U(1) ~= S1
coherent_state        = (|N> + exp(i phi)|S>)/sqrt(2)
bloch_location        = equator z=0
pole_exchange         = phi -> -phi
exchange_fixed_phases = {0, pi}
winding               = (1/2pi) integral dphi
phase_closure          = exp(i n phi)=1
```

This packet extends the earlier scalar half-seam without changing its exact base result.

## 10. Claim classes

| Statement | TIR class |
|---|---|
| pure normalized two-state family parameterization | EXACT |
| probability projection forgets relative phase | EXACT |
| fiber over \((1/2,1/2)\) is \(U(1)\cong S^1\) | EXACT |
| Bloch vector has unit norm | EXACT |
| half-seam maps to Bloch equator | EXACT |
| pole exchange acts as \(\varphi\mapsto-\varphi\) projectively | EXACT |
| exchange-fixed phases are \(0,\pi\) | EXACT |
| winding is integer for closed phase loops | STANDARD TOPOLOGICAL/GEOMETRIC RESULT |
| \(n\)-fold complex phase closure yields discrete phase fractions | EXACT CONDITIONAL |
| continuous unitary phase flow has a generator | STANDARD FUNCTIONAL-ANALYTIC THEOREM |
| physical-time Hamiltonian identification gives Schrödinger evolution | EXACT CONDITIONAL |
| incompatible Pauli axes are noncommuting | EXACT MATRIX IDENTITY |

## 11. Next gate

The next TIR foundation gate is now sharper than a generic “phase law”:

\[
\boxed{
\text{What selects }\dot\varphi\text{ on the half-fiber?}
}
\]

Candidate parents must come from the declared TIR axioms or an explicitly introduced dynamical postulate. This gate is the precise bridge from static first distinction to dynamical phase evolution.
