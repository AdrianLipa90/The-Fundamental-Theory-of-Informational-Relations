% !TEX root = ../metatime_monograph.tex
\chapter{The Metatime Framework}
\label{ch:metatime_framework}

% CITATION_CONTEXT_V10_9_BEGIN
\paragraph{Established context.}
The standard geometric ingredients behind phase holonomy, quantum-state metrics,
and cyclic evolution are described by Pancharatnam, Simon, Berry,
Aharonov--Anandan, and Provost--Vall\'ee
\cite{Pancharatnam1956,Simon1983,berry1984,AharonovAnandan1987,ProvostVallee1980}.
Connections to information geometry use the conventional Fisher--Rao and
Amari--Nagaoka setting \cite{Fisher1925,Rao1945,AmariNagaoka2000}. The
three-flavour mixing carrier is developed inside the TIR family branch.
% CITATION_CONTEXT_V10_9_END

\section{The Information Constant \(\kappa\)}

The current TIR dependency graph derives
\begin{equation}
\boxed{\kappa=\frac{\ln2}{24\pi}}.
\label{eq:kappa_framework_definition}
\end{equation}
Appendix~\ref{app:kappa} records the complete flavour-mixing normalization,
while Appendix~\ref{app:information_spinor_crosswalk} records its relation to
the half-axis, normalized-cycle and holonomy layers.

\claimstatus{TIR-internal derived structural normalization.}

\subsection{Binary Shannon numerator}

For the balanced binary distinction, Shannon entropy is \cite{shannon1948}
\begin{equation}
H\!\left(\frac12,\frac12\right)=\ln2
\quad\text{(nats)}.
\label{eq:shannon_binary}
\end{equation}
Thus
\[
\boxed{I_\star=\ln2}
\]
is the informational numerator of the normalization.

\subsection{Three-flavour mixing multiplicity}

The TIR family branch supplies the three-flavour carrier
\[
V_F\cong\mathbb C^3
\]
with full family mixing in
\[
U_F\in SU(3)_F.
\]
For a special unitary group,
\[
\dim_{\mathbb R}\mathfrak{su}(N)=N^2-1,
\]
so the three-flavour mixing algebra has
\begin{equation}
\boxed{\dim_{\mathbb R}\mathfrak{su}(3)_F=3^2-1=8.}
\label{eq:su3f-eight-directions}
\end{equation}
Combining the eight independent generator directions with the three flavour
labels gives the generator--flavour incidence count
\begin{equation}
\boxed{
N_{\rm mix}
=3\,\dim\mathfrak{su}(3)_F
=3\cdot8
=24.
}
\label{eq:mixing-channel-count}
\end{equation}
The later symmetric-pair decomposition
\[
\mathfrak{su}(3)_F=\mathfrak{so}(3)\oplus\mathfrak p,
\qquad
8=3+5,
\]
provides an independent carrier-level certificate of the same eight-dimensional
mixing algebra.

\subsection{Geometric-phase half-turn unit}

For a spin-$1/2$ eigenstate on the Bloch sphere, the standard geometric phase is
\begin{equation}
\gamma=-\frac{\Omega}{2}\pmod{2\pi},
\label{eq:berry_spin_half}
\end{equation}
where $\Omega$ is the enclosed solid angle \cite{berry1984}. A loop enclosing a
hemisphere ($|\Omega|=2\pi$) gives a phase of magnitude $\pi$ modulo $2\pi$.
A full-sphere solid angle $4\pi$ instead gives magnitude $2\pi$.

Independently, the primitive balanced coordinate supplies a half-turn of the
standard angular closure:
\begin{equation}
\boxed{
\Delta\phi_{1/2}=\frac12(2\pi)=\pi.
}
\label{eq:primitive-half-turn-phase}
\end{equation}
This is the phase unit assigned to each primitive generator--flavour incidence
channel in the normalization surface.

\subsection{The \(24\pi\) mixing-phase measure}

Equations~\eqref{eq:mixing-channel-count} and
\eqref{eq:primitive-half-turn-phase} give
\begin{equation}
\boxed{
\Phi_{\rm mix}
=N_{\rm mix}\Delta\phi_{1/2}
=24\pi.
}
\label{eq:mixing-phase-measure}
\end{equation}
Equivalently,
\begin{equation}
\boxed{
24\pi
=\underbrace{(3^2-1)}_{8\;SU(3)_F\;\mathrm{mixing\ directions}}
\underbrace{3}_{\mathrm{flavours}}
\underbrace{\pi}_{\mathrm{half\!\!-turn\ phase}}.
}
\label{eq:twentyfour-crossfactor}
\end{equation}
The exact integer identity
\[
24=8\cdot3=12\cdot2=6\cdot4
\]
is retained as a cross-factorization. Its canonical normalization provenance in
this chapter is the $8\cdot3$ flavour-mixing route.

\subsection{Structural derivation of \(\kappa\)}

Dividing the balanced binary information quantum by the primitive mixing-phase
measure gives
\begin{equation}
\boxed{
\kappa
=\frac{I_\star}{\Phi_{\rm mix}}
=\frac{\ln2}{3(3^2-1)\pi}
=\frac{\ln2}{24\pi}.
}
\label{eq:kappa_construction}
\end{equation}
Numerically,
\[
\kappa=0.009193150006360484\ldots .
\]

The local spatial branch supplies an independent finite-symmetry crosscheck:
for the regular tetrahedral cell,
\[
\operatorname{Aut}(\Delta^3)\cong S_4,
\qquad |S_4|=24,
\]
so
\[
\boxed{
3\dim\mathfrak{su}(3)_F
=24
=|S_4|.
}
\]
The flavour-mixing route supplies the normalization dependency; the tetrahedral
automorphism order supplies a separate convergence on the same integer.

\subsection{Normalized recurrence before radians}

A cyclic process may be represented first by a normalized turn coordinate
\begin{equation}
q\in\mathbb R/\mathbb Z,
\end{equation}
so that one recurrence is $q\mapsto q+1$. Its winding rate is
$f_q=\Delta N/\Delta t$. An angular coordinate is then introduced by
\begin{equation}
\phi=Cq,
\end{equation}
with standard radian closure $C=2\pi$.

The derived normalization implies
\begin{equation}
2\pi\kappa=\frac{\ln2}{12},
\label{eq:info-per-projective-turn}
\end{equation}
so the normalized-turn representation recovers the earlier twelve-cycle
factorization as a consequence of the $3\times8$ mixing normalization and the
half-turn decomposition.

\section{Exact phase-rate consequence}

If $\phi$ is an angular phase coordinate and
\begin{equation}
\omega=\frac{d\phi}{dt}=2\pi f,
\end{equation}
while
\begin{equation}
d\mathcal I=\kappa\,d\phi,
\end{equation}
then
\begin{equation}
\boxed{
\Gamma_{\mathcal I}
\equiv\frac{d\mathcal I}{dt}
=\kappa\omega
=\frac{\ln2}{12}f.
}
\label{eq:metatime_phase_rate_summary}
\end{equation}
The full constraint-manifold statement and its one-effective-degree-of-freedom
count are given in Appendix~\ref{app:kappa-constraint-manifold}. Operational
calibration of $\Gamma_{\mathcal I}$ is tracked by the empirical layer.

\section{The Exponential Mass Formula}

A central Metatime ansatz for selected mass relations is
\begin{equation}
m = E_P \, e^{-S/\kappa}.
\label{eq:mass_formula}
\end{equation}
Here $E_P = \sqrt{\hbar c^5 / G} = 1.22089 \times 10^{22}$~MeV is the Planck
energy, and $S$ is a dimensionless action constructed from declared
L-constants, arithmetic assignments and sector-specific rules. The model is
audited sector by sector under the repository claim hierarchy.

\claimstatus{Model ansatz; physical validity determined sector by sector.}

\subsection{Why the Exponential Form?}

The exponential architecture is motivated by comparison with statistical
weights such as
\[
Z = \sum_i e^{-E_i / k_B T}.
\]
In the Metatime construction, $\kappa$ sets a dimensionless information-phase
scale and $S$ is used as an action-like coordinate. Agreement or failure of the
resulting mass relations is tracked empirically in the validation ledgers.

\section{Action Construction Principles}

The current model builds sector actions $S$ from combinations of:
\begin{enumerate}
\item \textbf{Spin-labelled terms:} terms involving $1/2$ in fermionic sector assignments.
\item \textbf{Discrete structural terms:} contributions generated from the declared $L_i$ and flavour-label rules.
\item \textbf{Geometric/holonomic terms:} terms whose provenance is supplied by the corresponding relation and path geometry.
\item \textbf{Sector-specific release rules:} mappings whose empirical status is audited in their dedicated validation surfaces.
\end{enumerate}
