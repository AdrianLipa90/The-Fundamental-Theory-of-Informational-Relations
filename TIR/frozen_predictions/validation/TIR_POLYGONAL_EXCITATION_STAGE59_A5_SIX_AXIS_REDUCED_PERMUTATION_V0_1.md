# TIR Polygonal Excitation — Stage 59 A5 Six-Axis Reduced Permutation v0.1

Status: `STAGE_59_A5_SIX_AXIS_REDUCED_PERMUTATION_FIVE_PASS`

## Purpose

Stage 58 constructs six quadrupoles from the six antipodal axes of the regular icosahedron and shows that they form a regular 5-simplex spanning the complete five-dimensional symmetric-traceless carrier.

Stage 59 identifies the exact finite-group representation carried by the six axis labels themselves.

No CKM entries, masses, fitted coefficients, or amplitude kernels are used.

## Six-axis permutation representation

The rotational symmetry group of the regular icosahedron is

```math
A_5,
```

of order `60`.

Its action on the six antipodal vertex axes gives a permutation representation

```math
\rho_6:A_5\to S_6.
```

The executable reconstructs all 60 orientation-preserving icosahedral rotations directly from the standard vertex set and verifies that the induced action on the six axes is faithful.

## Fixed-axis character

For the conjugacy classes grouped by element order, the six-axis permutation character is

```text
identity: 6 fixed axes
order 2:  2 fixed axes
order 3:  0 fixed axes
order 5:  1 fixed axis
```

Equivalently, on the standard `A5` class ordering `(1A,2A,3A,5A,5B)`,

```math
\chi_{6}=(6,2,0,1,1).
```

The constant vector

```math
(1,1,1,1,1,1)
```

spans a trivial invariant representation. Removing it gives

```math
\chi_{6}-\chi_{1}
=(5,1,-1,0,0).
```

This is exactly the five-dimensional irreducible character of `A5`:

```math
\boxed{\chi_{6}-\chi_1=\chi_5.}
```

Its character norm is

```math
\frac1{60}
\left(1\cdot25+15\cdot1+20\cdot1+24\cdot0\right)
=1,
```

confirming irreducibility.

Therefore

```math
\boxed{\mathbb R^6\cong\mathbf1\oplus\mathbf5}
```

for the six-axis permutation carrier.

## Equivariant quadrupole map

Let `e_a` denote the standard basis of `R^6`, and let

```math
Q_a=u_au_a^T-\frac13I
```

be the Stage 58 quadrupole associated with axis `a`.

Define

```math
T(e_a)=Q_a.
```

Because

```math
\sum_{a=1}^6 Q_a=0
```

and the quadrupoles have rank five, the kernel of `T` is exactly the trivial line

```math
\ker T=\langle(1,1,1,1,1,1)\rangle.
```

For every icosahedral rotation `g`,

```math
T(\rho_6(g)e_a)
=gQ_ag^T.
```

Thus `T` descends to an `A5`-equivariant isomorphism

```math
\boxed{
\mathbb R^6/\mathbf1
\cong
\operatorname{Sym}^2_0(\mathbb R^3)
\cong
\mathbf5_{A_5}.
}
```

The executable obtains an equivariance residual of floating-point order `2e-16`.

## Relation to Stages 53--58

The same five-dimensional representation now has three explicit realizations:

```text
spin-2 carrier:                  Sym^2_0(R^3)
continuous symmetric-space part: tangent SU(3)/SO(3)
finite icosahedral realization:  R^6 / 1 on the six antipodal axes
```

Stage 58 supplies the explicit intertwiner through the quadrupoles `Q_a`.

## Separation from the earlier six-state lift

The six-axis representation in this stage is an `A5` action with faithful image of order `60` and includes elements of order `5`.

The earlier Stage 6 six-state construction is generated by the finite `C3 x Z2` / order-six operator structure.

Therefore these are distinct six-dimensional label actions and are retained separately.

## Evidential status

```text
icosahedral rotation group reconstructed: 60 elements
axis permutation image: 60 elements
permutation carrier dimension: 6
trivial invariant dimension: 1
reduced carrier dimension: 5
reduced character norm: 1
A5 equivariance: PASS
CKM input: NONE
mass input: NONE
fitted coefficients: NONE
```

## Reproducibility

Executable:

`TIR/frozen_predictions/validation/scripts/a5_six_axis_reduced_permutation_stage59_v01.py`

Receipt:

`TIR/frozen_predictions/validation/results/TIR_POLYGONAL_STAGE59_A5_SIX_AXIS_REDUCED_PERMUTATION_RECEIPT_V0_1.json`
