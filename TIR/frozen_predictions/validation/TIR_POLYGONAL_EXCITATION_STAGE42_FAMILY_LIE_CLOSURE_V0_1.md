# TIR Polygonal Excitation — Stage 42 Family Lie Closure v0.1

Status: `STAGE_42_SU3F_LIE_CLOSURE_PASS`

## Scope

This gate determines the real Lie algebra generated by the frozen family operators already established on the polygonal branch.

No CKM entries, masses, or fitted coefficients are used.

## Generators

Use the traceless skew-Hermitian forms of three Hermitian family operators:

1. ordered polygonal axis

```math
D=\operatorname{diag}\left(-\frac13,0,\frac1{\sqrt5}\right),
```

2. `C3` character-oriented axis

```math
C=F_3DF_3^\dagger,
```

3. seed-incidence adjacency

```math
A_{seed}=\begin{pmatrix}
0&1/2&0\\
1/2&0&0\\
0&0&0
\end{pmatrix}.
```

For each Hermitian `H`, the Lie generator is

```math
X_H=i\left(H-\frac{\operatorname{tr}H}{3}I\right).
```

The closure is generated by repeated commutators over the real vector space of traceless skew-Hermitian `3x3` matrices.

## Results

The executable obtains the following real Lie dimensions:

```text
{D, A_seed}:        4
{D, C}:             8
{A_seed, C}:        8
{D, A_seed, C}:     8
```

The structural residuals for skew-Hermiticity and zero trace remain at floating-point order `4e-17` to `6e-17`.

Since

```math
\dim_{\mathbb R}\mathfrak{su}(3)=8,
```

the result is

```math
\boxed{
\operatorname{Lie}\langle iD_0,iC_0\rangle
=\mathfrak{su}(3)_F.
}
```

The same full closure is retained after adding the seed-incidence operator.

## Role of the incidence operator

The seed-incidence adjacency is not required for algebraic `su(3)_F` closure. Its independent structural role is channel selection:

```math
A_{seed,12}=A_{seed,21}=\frac12,
```

while the `13` and `23` incidence entries vanish.

Thus the current frozen structure separates two mathematical functions:

```text
D + C3-character orientation -> full su(3)_F Lie closure
seed incidence               -> distinguished 1<->2 channel
```

## Consequence

The family carrier now has a closed eight-dimensional `su(3)_F` operator algebra generated from the existing polygonal/character geometry.

A unique physical family transformation requires an additional selection rule specifying the ordered evolution, action, or holonomy path inside this algebra. Stage 40 shows that one lowest-order affine choice fails the full CKM shape, so that failure remains retained.

## Evidential status

```text
Lie algebra dimension: 8
su(3)_F closure: PASS
continuous fitting: NONE
CKM input: NONE
mass input: NONE
physical evolution/selection rule: OPEN
```

## Reproducibility

Executable:

`TIR/frozen_predictions/validation/scripts/family_lie_closure_stage42_v01.py`

Receipt:

`TIR/frozen_predictions/validation/results/TIR_POLYGONAL_STAGE42_FAMILY_LIE_CLOSURE_RECEIPT_V0_1.json`

## Next gate

Search the existing TIR operator/action layer for a pre-existing ordering principle that can select a specific path in the generated `SU(3)_F` rather than an arbitrary group element. Candidate admissible sources are frozen holonomy path ordering, Euler-Berry action ordering, or the already declared seed/Collatz transition order. The selection rule must be recorded before a new CKM comparison.
