# The Space of Geometry — Research Spine v0.11

Status: `TIR_SPACE_OF_GEOMETRY_LENGTH_CALIBRATION_FRONTIER_V0_11_CANDIDATE`

## 1. Local geometry retained

The established local relation carrier remains

\[
\boxed{
\mathbb C^2
\to
\rho_x
\to
\mathcal A_2
\to
\delta(\rho_x,\rho_y)
\to
V=\operatorname{Herm}_0(2)\cong\mathbb R^3.
}
\]

With generator normalization,

\[
\boxed{
\mathcal E_{xy}=2(\rho_y-\rho_x)
=(\mathbf r_y-\mathbf r_x)\cdot\boldsymbol\sigma,
}
\]

and invariant local metric

\[
\boxed{
g_0(A,B)=\frac12\operatorname{Tr}(AB).
}
\]

The physical one-edge relation domain is

\[
\boxed{
\mathcal R_{\rm phys}
=\{\mathbf d\cdot\boldsymbol\sigma:\ |\mathbf d|\le2\}.
}
\]

The Euclidean, physical-realizability, finite-cell and SIC branches of v0.10 remain unchanged.

## 2. The calibration theorem

The local rotational structure fixes the positive Euclidean inner product uniquely up to one positive factor. Introduce a physical length calibration parameter

\[
L_*>0,
\qquad [L_*]=L.
\]

Then every calibrated Euclidean metric compatible with the established local `SO(3)` action is

\[
\boxed{
g_{\rm phys}=L_*^2g_0.}
\]

The physical relation length is therefore

\[
\boxed{
\ell_{\rm phys}(\mathcal E)
=L_*\sqrt{\frac12\operatorname{Tr}(\mathcal E^2)}.
}
\]

Thus the unresolved physical-scale content of the local geometry is one positive scalar source.

## 3. Exact calibration inversion

For any nonzero reference relation with

\[
q_{\rm ref}=\frac12\operatorname{Tr}(\mathcal E_{\rm ref}^2)>0
\]

and typed physical length `ell_ref`,

\[
\boxed{
L_*=\frac{\ell_{\rm ref}}{\sqrt{q_{\rm ref}}}.
}
\]

A single physical length datum therefore selects a unique representative from the one-parameter scale family.

## 4. Physical one-edge radius after calibration

Since

\[
|\mathbf d|\le2,
\]

the exact calibrated one-edge domain is

\[
\boxed{
0\le\ell_{\rm phys}\le2L_*.
}
\]

Hence

\[
\boxed{D_{\rm edge}=2L_*}
\]

and equivalently

\[
\boxed{L_*=D_{\rm edge}/2.}
\]

This gives a direct bridge from any later physical edge-diameter law to the local spatial unit.

## 5. Scale-invariant outputs

The following established outputs survive the calibration exactly:

\[
\boxed{
\cos\theta
=\frac{g_0(A,B)}{\sqrt{g_0(A,A)g_0(B,B)}}
=\frac{g_{\rm phys}(A,B)}{\sqrt{g_{\rm phys}(A,A)g_{\rm phys}(B,B)}}
}
\]

and therefore orthogonality is unchanged.

Length ratios satisfy

\[
\boxed{
\frac{\ell_{\rm phys}(A)}{\ell_{\rm phys}(B)}
=\frac{\|A\|_0}{\|B\|_0}.
}
\]

Pythagorean closure becomes

\[
\boxed{
\ell_{\rm phys}(A+B)^2
=\ell_{\rm phys}(A)^2+\ell_{\rm phys}(B)^2
}
\]

whenever `A` and `B` are orthogonal.

The normalized tetrahedral Gram invariant remains

\[
\boxed{n_a\cdot n_b=-\frac13.}
\]

Thus local shape, angle, ratio and finite-frame structure are separated cleanly from the physical choice of length unit.

## 6. Dimensional source gate

The structural numbers already present in the local geometry branch -- `1/2`, `ln 2`, `kappa`, group dimensions, rational Gram coefficients and representation normalizations -- are dimensionless. They may enter a dimensionless coefficient multiplying a physical scale.

Any source proposed for the physical calibration is typed by

\[
\boxed{[L_*]=L.}
\]

The next theorem must therefore bind `L_*` to a parent-TIR, matter-sector, temporal, experimental, or mixed dimensional object whose resulting dimension is length.

This converts the former broad phrase `physical unit calibration` into the precise gate

\[
\boxed{\texttt{DERIVE_OR_BIND_L_STAR_SOURCE}.}
\]

## 7. Updated dependency graph

The local geometry programme is now

\[
\boxed{
\begin{aligned}
\mathbb C^2
&\to V=\operatorname{Herm}_0(2)\cong\mathbb R^3\\
&\to
\begin{cases}
\text{Euclidean branch}\to\text{Pythagoras},\\
\text{physical chord branch}\to\text{physical Pythagoras},\\
\text{finite-cell branch}\to\text{regular tetrahedron}\leftarrow\text{SIC},
\end{cases}\\
&\to g_{\rm phys}=L_*^2g_0
\to \text{dimensionful local length geometry}.
\end{aligned}
}
\]

The calibration node depends on the already-established metric and physical relation domain but does not alter the proof dependencies of Theorems E, R, T or Q.

## 8. Current frontier

The local structural problem has been reduced to two downstream tasks:

1. source-bind `L_*` through a dimensionally typed physical law;
2. glue calibrated local carriers into extended spatial complexes with transport, holonomy and curvature.

The first task fixes the local physical unit. The second turns the local carrier into global spatial extent.
