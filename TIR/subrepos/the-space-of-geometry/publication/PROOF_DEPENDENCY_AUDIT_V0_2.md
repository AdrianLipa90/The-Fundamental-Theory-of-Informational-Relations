# The Space of Geometry — Proof Dependency Audit v0.2

Status: `TIR_SPACE_OF_GEOMETRY_PUBLICATION_PROOF_AUDIT_V0_2_PHYSICAL_REALIZABILITY`

Purpose: extend the v0.1 publication DAG with an exact firewall between the full affine translation carrier and the bounded relation subset generated by pairs of physical binary density states, and certify an explicit physical realization of the Pythagorean endpoint.

## 1. Node classes

The publication chain uses five classes.

- `IMPORT`: result imported from the parent TIR foundations.
- `EXACT`: algebraic, affine, linear-algebraic, group-theoretic, Euclidean, or convex-state theorem.
- `AXIOM_CROSSWALK`: a TIR axiom supplies the declared geometric role of an exact mathematical structure.
- `PHYSICAL_REALIZATION`: an exact construction whose endpoints remain inside the physical density-state domain.
- `CROSSCHECK`: independent convergence used as corroboration, not as a premise of the main endpoint.

## 2. Canonical nodes

`P0` — imported binary quantum carrier

\[
\mathcal H_2\cong\mathbb C^2.
\]

`C1` — physical Bloch domain and affine hull

\[
\rho(\mathbf r)=\frac12(I+\mathbf r\cdot\boldsymbol\sigma),
\qquad |\mathbf r|\le1,
\]

\[
\mathcal A_2=\frac12I+\operatorname{Herm}_0(2),
\qquad
\dim_{\mathbb R}\operatorname{Herm}_0(2)=3.
\]

`C2` — canonical affine displacement

\[
\delta_{xy}=\rho_y-\rho_x.
\]

`C3` — generator-normalized relation and covariance

\[
\mathcal E_{xy}=2(\rho_y-\rho_x),
\qquad
PSU(2)\cong SO(3).
\]

`C4` — invariant positive inner product

\[
\langle A,B\rangle=\frac12\operatorname{Tr}(AB).
\]

`R1` — exact physical single-edge relation domain

\[
\boxed{
\mathcal R_{\rm phys}
=\{\mathbf d\cdot\boldsymbol\sigma:\ |\mathbf d|\le2\}.
}
\]

`E1` — additive endpoint composition

\[
\mathcal E_{xz}=\mathcal E_{xy}+\mathcal E_{yz}.
\]

`E2` — norm polarization identity

\[
\|A+B\|^2=\|A\|^2+\|B\|^2+2\langle A,B\rangle.
\]

`E3` — carrier-level Pythagorean closure under orthogonality

\[
\boxed{a^2+b^2=c^2}.
\]

`R2` — physical right-triangle family. For orthogonal unit `u,v` and

\[
a^2+b^2\le1,
\]

choose

\[
\mathbf r_x=0,
\qquad
\mathbf r_y=a\mathbf u,
\qquad
\mathbf r_z=a\mathbf u+b\mathbf v.
\]

All three states are physical.

`E4` — physical Pythagorean realization

\[
\boxed{
\|\mathcal E_{xz}\|^2
=\|\mathcal E_{xy}\|^2
+\|\mathcal E_{yz}\|^2.
}
\]

The exact normalized certificate is

\[
\boxed{
\frac9{25}+\frac{16}{25}=1.
}
\]

`T1` — minimal full-dimensional finite support in a real three-dimensional affine carrier requires four vertices:

\[
\Delta^3.
\]

`T2` — intrinsic simplex automorphism structure

\[
\operatorname{Aut}(\Delta^3)\cong S_4
\]

with one six-edge orbit.

`T3` — A5+A7 edge-measure invariance on that intrinsic orbit.

`T4` — six equal edge measures imply the regular tetrahedron.

`Q1` — independent tetrahedral qubit SIC/tight-frame convergence

\[
n_a\cdot n_b=-\frac13\qquad(a\ne b).
\]

## 3. Active dependency edges

```text
P0 -> C1
C1 -> C2
C2 -> C3
C3 -> C4
C1 -> R1
C3 -> R1
C2 -> E1
C4 -> E2
E1 -> E3
E2 -> E3
R1 -> R2
C4 -> R2
E1 -> R2
R2 -> E4
E3 -> E4
C1 -> T1
T1 -> T2
C4 -> T3
T2 -> T3
T3 -> T4
C1 -> Q1
T4 -> Q1
```

The graph is acyclic.

## 4. Euclidean theorem branch

The carrier-level theorem remains

\[
\boxed{
P0\to C1\to C2\to(C3,C4,E1,E2)\to E3.
}
\]

No tetrahedral node is an ancestor of `E3`. Thus Pythagorean closure remains independent of the finite-cell theorem.

## 5. Physical-realizability branch

The new publication hardening is

\[
\boxed{
(C1,C3)\to R1
\to R2
\to E4,
}
\]

with `C4` supplying the metric and `E1` supplying endpoint composition.

The physical single-edge domain is exactly the radius-two Bloch chord ball because

\[
|\mathbf r_y-\mathbf r_x|\le2
\]

for physical endpoints, while every \(|\mathbf d|\le2\) is realized by

\[
\mathbf r_x=-\mathbf d/2,
\qquad
\mathbf r_y=+\mathbf d/2.
\]

Therefore the three-dimensional carrier is locally populated by physical relation vectors in every direction.

For the right-triangle family, `a^2+b^2<=1` keeps all three endpoints inside the Bloch ball. Hence `E4` certifies that the Pythagorean identity has an explicit physical-state realization rather than existing only as an identity on the unrestricted affine hull.

## 6. Finite-cell branch

The finite-cell branch remains

\[
\boxed{
C1\to T1\to T2\to T3\to T4.
}
\]

Its regularity proof uses the exact intrinsic `S_4` edge orbit plus the declared A5+A7 edge-measure law crosswalk. Moment isotropy and SIC geometry remain consequences/crosschecks rather than active premises.

## 7. SIC firewall

`Q1` is a `CROSSCHECK`. It is not an ancestor of `E3`, `E4`, or `T4`.

The spatial tetrahedral frame and the symmetric qubit informational frame share the exact Gram relation

\[
\boxed{n_a\cdot n_b=-\frac13,}
\]

while the publication keeps their physical typing separate.

## 8. Carrier-domain firewall

The following objects are distinct and explicitly typed:

\[
\boxed{
V=\operatorname{Herm}_0(2)\cong\mathbb R^3
}
\]

is the full affine translation carrier;

\[
\boxed{
\mathcal R_{\rm phys}\cong\mathbb B^3_2
}
\]

is the bounded set of single-edge relations realized by physical endpoints in one binary state fiber.

The bounded physical chord domain does not reduce the carrier dimension because it contains a neighborhood of zero and spans all three directions.

## 9. Scale and global-extent firewall

The factor `2` in

\[
\mathcal E_{xy}=2(\rho_y-\rho_x)
\]

is the Pauli/Bloch generator normalization. Physical unit calibration remains downstream.

The radius-two physical chord bound is local to one binary state fiber. Global spatial extent belongs to the downstream gluing/bundle programme relating distinct local carriers.

## 10. Publication verdict

The hardened v1.0 local dependency graph has three controlled outputs from one common carrier:

\[
\boxed{
V
\to
\begin{cases}
\text{Euclidean identity}\to E3,\\
\text{physical-state realization}\to E4,\\
\text{minimal finite support}\to T4.
\end{cases}
}
\]

The tetrahedral SIC is an independent convergence crosscheck.

Publication status under this audit:

`PASS_LOCAL_DEPENDENCY_GRAPH_AND_PHYSICAL_REALIZABILITY`.
