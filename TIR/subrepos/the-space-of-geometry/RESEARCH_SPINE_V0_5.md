# The Space of Geometry — Research Spine v0.5

Status: `TIR_SPACE_OF_GEOMETRY_SPINE_V0_5_CANDIDATE`

## 1. Shortest local chain

\[
\boxed{
\text{FIRST DISTINCTION}
\to
\mathbb C^2
\to
\rho_x
\to
\mathcal A_2=\frac12I+\operatorname{Herm}_0(2)
\to
\text{source-minimal intrinsic relation}
\to
\rho_y-\rho_x
\to
\operatorname{Herm}_0(2)\cong\mathbb R^3
\to
\Delta^3
\to
\text{regular tetrahedral frame}
\to
\text{metric / angle / orthogonality}
\to
\boxed{a^2+b^2=c^2}.
}
\]

## 2. Exact affine carrier

The trace-one Hermitian two-level state space has affine hull

\[
\mathcal A_2=\frac12I+V,
\qquad
V=\operatorname{Herm}_0(2),
\qquad
\dim_{\mathbb R}V=3.
\]

For every ordered pair there is a unique intrinsic torsor displacement

\[
\delta(\rho_x,\rho_y)=\rho_y-\rho_x.
\]

In Pauli normalization,

\[
\mathcal E_{xy}=2(\rho_y-\rho_x).
\]

## 3. Source-minimality theorem surface

The primitive relation-law signature contains only

\[
(\rho_x,\rho_y;\mathcal A_2,V).
\]

If no independent origin, axis, basis or background tensor is introduced at this layer, the relation must be natural under affine automorphisms

\[
F(x)=Lx+a.
\]

Hence

\[
R(Fx,Fy)=L R(x,y).
\]

Every continuous affine-natural vector relation is then

\[
\boxed{R(x,y)=c(y-x)}.
\]

Distinction preservation gives

\[
\boxed{c\ne0}.
\]

The torsor interpretation fixes `c=1`; the established Pauli generator convention displays the same displacement with factor `2` relative to density-operator difference.

## 4. Remaining TIR inheritance gate

The mathematical relation bridge is now exact after one dependency rule:

\[
\boxed{
\texttt{A1\_DEPENDENCY\_MINIMALITY\_APPLIES\_TO\_PRIMITIVE\_LAW\_SIGNATURE}.
}
\]

Operationally this asks whether A1 minimality applies not only to the primitive carrier but also to the primitive law signature, so that a first-level physical relation may use only structure already sourced upstream.

Together with A3 relational primacy this gives

\[
\text{source-minimal relation}
\to
\text{affine naturality}
\to
R(x,y)=c(y-x),\ c\ne0.
\]

No additional geometric object is required at this local stage.

## 5. Three dimensions and the tetrahedron

Because

\[
V\cong\mathbb R^3,
\]

the minimal full-dimensional affine simplex has four vertices:

\[
\boxed{\Delta^3}.
\]

With the local symmetry/equivalence condition already registered in the tetrahedral branch, the centered unit directions satisfy

\[
\sum_{a=1}^4 n_a=0,
\qquad
n_a\cdot n_b=-\frac13\quad(a\ne b),
\]

which is the regular tetrahedral frame.

The same frame remains an independent qubit informational-completeness convergence check.

## 6. Euclidean metric and Pythagorean closure

The invariant quadratic form is unique up to scale under the irreducible `SO(3)` action. In the established generator normalization,

\[
\boxed{
\langle A,B\rangle=\frac12\operatorname{Tr}(AB).
}
\]

Torsor endpoint composition gives

\[
\mathcal E_{xz}=\mathcal E_{xy}+\mathcal E_{yz}.
\]

Therefore

\[
\|\mathcal E_{xz}\|^2
=
\|\mathcal E_{xy}\|^2
+
\|\mathcal E_{yz}\|^2
+
2\langle\mathcal E_{xy},\mathcal E_{yz}\rangle.
\]

For orthogonal consecutive relations,

\[
\langle\mathcal E_{xy},\mathcal E_{yz}\rangle=0,
\]

so

\[
\boxed{a^2+b^2=c^2}.
\]

## 7. Role split of TIR axioms

The local derivation now uses the axioms in a narrow dependency order:

\[
A1:\ \text{minimal primitive source signature},
\]

\[
A2:\ \text{quantum point carrier},
\]

\[
A3:\ \text{physical structure carried by distinguishable relations},
\]

\[
A5:\ \text{arithmetic measurement of geometric invariants},
\]

\[
A7:\ \text{symmetry of the relation law / rotational invariance},
\]

while A8 remains the downstream context-lift/closure mechanism when multiple local frames or nontrivial holonomy are compared.

## 8. Paper boundary

The fundamental paper remains local and stops at Pythagorean closure. Global triangulation, curvature, holonomy, torsion sectors and the TIR x Time spacetime join remain downstream programmes.
