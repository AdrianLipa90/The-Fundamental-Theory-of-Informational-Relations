# Constants Falsification

Genesis of constants fails if the system cannot produce at least one nontrivial dimensionless constant from arithmetic + geometry + closure without injecting its numerical value.
