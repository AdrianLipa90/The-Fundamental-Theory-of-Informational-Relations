# Simulations

This folder contains simulation code and simulation results.

Every simulation must be registered in `registry.yaml` with:
- id
- title
- code path
- result path
- linked theory sections
- linked constants/modules
- status
