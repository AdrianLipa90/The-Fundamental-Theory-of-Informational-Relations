interface:
  id: IF-OPR-0002
  name: relational_information_contract
  required_inputs:
    - sector_projected_information
    - A_ij
    - W_ij
    - gamma_i
    - relational_coupling_parameter
  required_outputs:
    - relational_information_operator
    - diagonal_information_block
    - off_diagonal_coupling_block
    - closure_ready_phase_structure
  rule: off-diagonal entries must remain explicitly traceable to relational objects and may not encode hidden fitted constants
