interface:
  id: IF-OPR-0005
  name: genesis_operator_contract
  required_inputs:
    - relational_time_operator
    - closure_invariants
    - sector_projection_rule
    - I0
    - Lambda0
    - correction_factor_policy
  required_outputs:
    - sector_exponents
    - correction_factors
    - global_scale_operator
    - sector_mass_blueprints
  rule: genesis outputs must separate dimensionless operator invariants from dimensional lifting steps
