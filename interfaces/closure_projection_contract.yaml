interface:
  id: IF-OPR-0003
  name: closure_projection_contract
  required_inputs:
    - holonomy_defect
    - closure_defect_scalar
    - admissibility_tolerance
    - relational_information_operator
  required_outputs:
    - closure_projector
    - physical_relational_information_operator
    - admissibility_mask
    - rejected_configuration_report
  rule: closure projection must expose the admissibility rule explicitly and cannot silently pass invalid configurations
