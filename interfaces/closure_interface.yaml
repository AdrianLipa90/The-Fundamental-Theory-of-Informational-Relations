id: IF-CLOSURE-001
name: Closure interface
inputs:
  - phase_vector
  - sector_weights
  - rollback_enabled
outputs:
  - closure_violation
  - accepted
  - rollback_applied
  - updated_phase_vector
invariants:
  - no_accepted_worsening_when_rollback_enabled
