interface:
  id: IF-OPR-0004
  name: time_extraction_contract
  required_inputs:
    - physical_relational_information_operator
    - time_extraction_rule
    - spectral_selection_policy
  required_outputs:
    - relational_time_operator
    - tau_spectrum
    - spectral_diagnostics
    - residual_assumptions
  rule: tau_i may not be declared first-principles derived unless their spectral origin is exposed in this contract
