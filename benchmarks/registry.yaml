version: 1
benchmarks:
  - id: BM-INIT-001
    module: initial_conditions
    metric: determinism
    description: identical inputs yield identical normalized Bloch states
  - id: BM-CLOSURE-001
    module: euler_closure
    metric: monotonic_nonworsening_with_rollback
    description: accepted steps should not worsen closure when rollback is enabled
