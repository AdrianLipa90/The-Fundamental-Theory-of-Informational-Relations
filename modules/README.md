# Modules

This folder is reserved for theory-facing module documentation that complements but does not replace `derivations/` and `src/ciel_foundations/`.

## Use
Store compact module-level design notes, module manifests, and human-readable module maps here when needed.

## Rule
Do not treat this folder as a parallel source of truth. Formal content must still resolve upstream to registries and derivations.
