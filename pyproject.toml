[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ciel-foundations"
version = "0.1.0"
description = "Foundational derivation project for CIEL"
requires-python = ">=3.11"
