# LaTeX

This directory stores publication material section by section. It is downstream of the source-of-truth markdown/YAML/code tree.

Subdirectories:
- `papers/` complete paper assemblies
- `sections/` reusable section source files
- `figures/` figures
- `tables/` reusable tables
- `macros/` shared commands and notation
- `bib/` bibliography build assets
