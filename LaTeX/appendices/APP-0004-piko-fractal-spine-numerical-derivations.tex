\appendix
\section{Numerical Appendix: Piko Fractal Foundations Spine}
\label{app:piko-fractal-foundations-spine}

\subsection{Registered snapshot}
The registered piko snapshot combines four previously established numerical checks:
\begin{enumerate}
  \item equatorial Berry phase $\gamma_B = \pi$,
  \item closure magnitude $R_H \approx 0$ for the pair $(0,\pi)$,
  \item tetrahedral relative phase triple $(0.4,1.2,-0.7)$,
  \item nonlocal holonomic vortex predicate equal to true for the registered closed spacetime loop.
\end{enumerate}

\subsection{Consistency score}
Define the toy consolidated score by averaging three binary/threshold checks:
\begin{equation}
S_{\mathrm{spine}} = \frac{1}{3}\left(\mathbf 1_{\{|R_H|\approx 0\}} + \mathbf 1_{\{|\gamma_B-\pi|\approx 0\}} + \mathbf 1_{\{\Gamma[\mathcal C]\neq 0\}}\right).
\end{equation}
For the registered toy snapshot,
\begin{equation}
S_{\mathrm{spine}} = 1.
\end{equation}

\subsection{Executable realization}
The corresponding executable modules are:
\begin{itemize}
  \item \texttt{src/ciel\_foundations/solvers/piko\_fractal\_spine\_solver.py}
  \item \texttt{tests/test\_piko\_fractal\_spine.py}
  \item \texttt{Simulations/code/sim\_piko\_fractal\_spine.py}
\end{itemize}
