# Lineage from v6.0B

Source archive: `METATIME_STANDARD_MODEL_DERIVATION_MERGED_REPO_v6_0B_INFORMATION_ENERGY_NORMALIZER_NO_NESTED_ZIPS.zip`

v6.0B formalized `E[I] = E0 * Phi(I)` and showed that `E0` is the missing dimensional normalizer. v6.1 adds Planck energy as the strongest current `E0` candidate and converts reference targets into required information-action values.

All target values are used only after the bridge is defined and only as ledger targets.
