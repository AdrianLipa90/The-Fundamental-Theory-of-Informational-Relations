# METATIME SM v4.9 — Short-Doc Charged-Lepton Kernel Freeze

## Status

- technical: PASS
- formal: PASS_WITH_STRICT_SHORT_DOC_SOURCE_LEDGER
- substantive: FROZEN_SHORT_DOC_KERNEL_NOT_NUMERIC_CLOSURE
- mass prediction: NOT CLAIMED
- benchmark: NOT PERFORMED
- Debt 9 numeric spectrum: OPEN_NOT_CLOSED
- canon/current promotion: DENIED

## Why v4.9 exists

v4.8 reduced the charged-lepton problem using the document F_ij table and tau_v2 gates, but it still selected the best diagnostic after looking at benchmark errors. v4.9 fixes the source ordering: it starts from the shorter step-docs and freezes only the clean kernel that those documents actually justify.

## Short-doc kernel extracted

The active short-doc ingredients are:

1. Collatz/Bloch/Fubini-Study normal form.
2. Twin-prime first-descent complementarity.
3. Preference quantum `ln2/(24π)`.
4. Berry-Euler origin of `24π = terminal C3 * parity sheet * 4π spinor closure`.
5. Ramanujan-theta residue as diagnostic weight.
6. Tetrahedral `C3` terminal cell.
7. Spinor half-phase / identity-holonomy residue.
8. F_ij toy S2 orientation table, recalculated with the exact short-doc `ln2/(24π)` rather than legacy `0.009`.

## Frozen formula

No MeV conversion is defined in v4.9. The frozen dimensionless kernel is:

```text
F_short(e,i) = exp((ln2/(24π)) * (theta_i - theta_e))
G_i = exp(0.5 * (tau_v2_action_i - tau_v2_action_e))
K_i = composite_document_tau_trace_v4_5_i * F_short(e,i) * G_i
```

The half-action factor is not chosen because it scored best in v4.8. It is now justified structurally by the short-doc spinor identity invariant: the identity-sensitive holonomy is written at half-phase because lifted spinor closure is `4π`, not merely projective `2π` closure.

## Important negative result

The strict short-doc F_ij is promille-scale. It cannot by itself justify the large charged-lepton screening/release table used diagnostically in v4.8. Therefore the large F table remains a clue, not a promoted derivation.

## Next step

v5.0 should not fit lepton masses. It should derive the strong charged-lepton `F_ij` / `W_ij` release factors from White-Thread holonomy or reject them. Only after such a derivation can a sealed benchmark be meaningful.
