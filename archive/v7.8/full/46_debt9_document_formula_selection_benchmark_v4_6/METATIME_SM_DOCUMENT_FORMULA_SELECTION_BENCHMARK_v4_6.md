# METATIME / Standard Model v4.6
## Document formula selection + sealed benchmark

Status: **PASS as sealed diagnostic; FAIL as numeric Debt 9 closure**.

v4.6 uses the user's document calculations as directly as possible while preserving the anti-fit guard. The selected clean formula is the one requested in the discussion:

```text
Collatz dynamics + twin-prime center p+1 + base L3=7
lambda_i = (L(center=p+1)/L3)^2
m_i = scale_family * lambda_i^alpha_family
```

The formula is selected before benchmark because it is the literal `p+1 / L3` route. It is then converted to MeV using one benchmark anchor per family only for diagnostic units. Therefore it is not a no-parameter mass prediction.

## Result

The selected clean formula does not reproduce the fermion mass hierarchy. It passes as a structural diagnostic but fails as a Debt 9 numeric closure.

The benchmark also compares additional predeclared formulas:

- lower twin-prime `p` Collatz length;
- document `tau_doc/eigenvalue` power law;
- v4.5 composite trace coordinate.

The document `tau_doc` power law gives good-looking heavy-quark numbers only because that sector is already labelled in the documents as a power-law anchor / fit. That remains useful as a clue but cannot be promoted.

## Taint boundary

Not promoted:

- charged-lepton Vandermonde polynomial;
- light-quark correction factors from benchmark ratios;
- heavy-quark fitted power law;
- neutrino global scale from observed splittings.

## Interpretation

The document route is real and structurally coherent. The literal `p+1 / L3` mass map is not yet the missing operator. The next step must not add arbitrary new structures; it should resolve the internal inconsistency between:

1. center seed `p+1`,
2. document `tau_doc/eigenvalue`,
3. generation-resolved Collatz length,
4. White-Thread / open holonomy correction,
5. Ramanujan scaling,
6. Yukawa bridge.

Debt 9 numeric spectrum remains **OPEN_NOT_CLOSED**.
