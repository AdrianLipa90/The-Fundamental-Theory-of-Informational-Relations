# Stage 13 — Euler--Berry Coherence Gate v1.3

This stage belongs to the merged Metatime Standard Model derivation repository.
It must not be distributed as a standalone stage archive.

Main decision: zeta--tetrahedral data are a coherence gate, not a raw additive mass cost.

Run:

```bash
python3 scripts/eulerberry_coherence_gate_v1_3.py
```

Outputs are written to `results/`.
