# Lineage from v5.1

Base: v5.1 White-Thread quadratic amplitude map candidate.

This module freezes the v5.1 root-relative gates into one charged-lepton benchmark formula. It does not use the failed all-pair F_ij table and does not tune after scoring.
