# METATIME SM — v6.1 Planck Information-Action Ledger

## Purpose

v6.0B showed that pure information cannot yield energy without a dimensional normalizer `E0`. v6.1 installs the strongest current `E0` candidate: Planck energy from non-mass physical constants.

## Core bridge

```text
E_i = E_P * exp(-S_I(i) / OI)
m_i = E_i / c^2
OI = ln2/(24π)
```

This does not derive masses. It converts reference energies into a target ledger of required information actions. The next derivation must produce these actions from Collatz/Ramanujan/OI geometry without using the target masses.

## Why Planck E0

Schumann and Landauer scales are dimensionally valid, but they are either too small for Standard Model scales without huge effective counts or environment-dependent. Planck energy is a clean dimensional normalizer from `c`, `hbar`, and `G`.

## Boundary

The module uses reference energies only as target ledgers. It does not fit `E_P`, does not fit `OI`, does not use NoParamSM, and does not claim Debt 9 closure.

## Next step

v6.2 should derive `S_I(i)` for charged leptons from the existing Collatz/Ramanujan/OI stack and compare to the v6.1 target ledger.
